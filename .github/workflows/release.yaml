name: Release
run-name: Release • main • ${{ github.sha }}

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  ci:
    name: Continuous Integration
    uses: $/.github/workflows/pr.yaml

  publish:
    name: Publish release
    needs: ci
    runs-on: ubuntu-slim
    timeout-minutes: 15

    permissions:
      contents: write
      pull-requests: write
      issues: write

    steps:
      # Credentials stay persisted: the later git fetch/checkout steps and
      # goreleaser operate against origin using the checkout's auth header.
      - name: Checkout repository
        uses: actions/checkout@v7 # zizmor: ignore[artipacked]
        with:
          fetch-depth: 0

      - name: Set up build environment
        uses: $/.github/actions/setup

      - name: Create GitHub release
        id: release
        uses: googleapis/release-please-action@v5
        with:
          config-file: release-please-config.json

      - name: Fetch tags
        run: git fetch --force --tags

      - name: Checkout release tag
        if: ${{ steps.release.outputs.release_created }}
        env:
          TAG_NAME: ${{ steps.release.outputs.tag_name }}
        run: git checkout "$TAG_NAME"

      - name: Publish release artifacts
        if: ${{ steps.release.outputs.release_created }}
        uses: goreleaser/goreleaser-action@v7
        with:
          version: latest
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GORELEASER_CURRENT_TAG: ${{ steps.release.outputs.tag_name }}
